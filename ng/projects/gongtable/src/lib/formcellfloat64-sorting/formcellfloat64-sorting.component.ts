// generated by gong
import { Component, OnInit, Inject, Optional } from '@angular/core';
import { TypeofExpr } from '@angular/compiler';
import { CdkDragDrop, moveItemInArray } from '@angular/cdk/drag-drop';

import { MatDialogRef, MAT_DIALOG_DATA, MatDialog } from '@angular/material/dialog'
import { DialogData } from '../front-repo.service'
import { SelectionModel } from '@angular/cdk/collections';

import { Router, RouterState } from '@angular/router';
import { FormCellFloat64DB } from '../formcellfloat64-db'
import { FormCellFloat64Service } from '../formcellfloat64.service'

import { FrontRepoService, FrontRepo } from '../front-repo.service'
import { NullInt64 } from '../null-int64'

@Component({
  selector: 'lib-formcellfloat64-sorting',
  templateUrl: './formcellfloat64-sorting.component.html',
  styleUrls: ['./formcellfloat64-sorting.component.css']
})
export class FormCellFloat64SortingComponent implements OnInit {

  frontRepo: FrontRepo = new (FrontRepo)

  // array of FormCellFloat64 instances that are in the association
  associatedFormCellFloat64s = new Array<FormCellFloat64DB>();

  constructor(
    private formcellfloat64Service: FormCellFloat64Service,
    private frontRepoService: FrontRepoService,

    // not null if the component is called as a selection component of formcellfloat64 instances
    public dialogRef: MatDialogRef<FormCellFloat64SortingComponent>,
    @Optional() @Inject(MAT_DIALOG_DATA) public dialogData: DialogData,

    private router: Router,
  ) {
    this.router.routeReuseStrategy.shouldReuseRoute = function () {
      return false;
    };
  }

  ngOnInit(): void {
    this.getFormCellFloat64s()
  }

  getFormCellFloat64s(): void {
    this.frontRepoService.pull(this.dialogData.GONG__StackPath).subscribe(
      frontRepo => {
        this.frontRepo = frontRepo

        let index = 0
        for (let formcellfloat64 of this.frontRepo.FormCellFloat64s_array) {
          let ID = this.dialogData.ID
          let revPointerID = formcellfloat64[this.dialogData.ReversePointer as keyof FormCellFloat64DB] as unknown as NullInt64
          let revPointerID_Index = formcellfloat64[this.dialogData.ReversePointer + "_Index" as keyof FormCellFloat64DB] as unknown as NullInt64
          if (revPointerID.Int64 == ID) {
            if (revPointerID_Index == undefined) {
              revPointerID_Index = new NullInt64
              revPointerID_Index.Valid = true
              revPointerID_Index.Int64 = index++
            }
            this.associatedFormCellFloat64s.push(formcellfloat64)
          }
        }

        // sort associated formcellfloat64 according to order
        this.associatedFormCellFloat64s.sort((t1, t2) => {
          let t1_revPointerID_Index = t1[this.dialogData.ReversePointer + "_Index" as keyof typeof t1] as unknown as NullInt64
          let t2_revPointerID_Index = t2[this.dialogData.ReversePointer + "_Index" as keyof typeof t2] as unknown as NullInt64
          if (t1_revPointerID_Index && t2_revPointerID_Index) {
            if (t1_revPointerID_Index.Int64 > t2_revPointerID_Index.Int64) {
              return 1;
            }
            if (t1_revPointerID_Index.Int64 < t2_revPointerID_Index.Int64) {
              return -1;
            }
          }
          return 0;
        });
      }
    )
  }

  drop(event: CdkDragDrop<string[]>) {
    moveItemInArray(this.associatedFormCellFloat64s, event.previousIndex, event.currentIndex);

    // set the order of FormCellFloat64 instances
    let index = 0

    for (let formcellfloat64 of this.associatedFormCellFloat64s) {
      let revPointerID_Index = formcellfloat64[this.dialogData.ReversePointer + "_Index" as keyof FormCellFloat64DB] as unknown as NullInt64
      revPointerID_Index.Valid = true
      revPointerID_Index.Int64 = index++
    }
  }

  save() {

    this.associatedFormCellFloat64s.forEach(
      formcellfloat64 => {
        this.formcellfloat64Service.updateFormCellFloat64(formcellfloat64, this.dialogData.GONG__StackPath)
          .subscribe(formcellfloat64 => {
            this.formcellfloat64Service.FormCellFloat64ServiceChanged.next("update")
          });
      }
    )

    this.dialogRef.close('Sorting of ' + this.dialogData.ReversePointer + ' done');
  }
}
