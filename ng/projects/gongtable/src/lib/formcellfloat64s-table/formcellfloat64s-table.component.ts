// generated by gong
import { Component, OnInit, AfterViewInit, ViewChild, Inject, Optional, Input } from '@angular/core';
import { BehaviorSubject } from 'rxjs'
import { MatSort } from '@angular/material/sort';
import { MatPaginator } from '@angular/material/paginator';
import { MatTableDataSource } from '@angular/material/table';
import { MatButton } from '@angular/material/button'

import { MatDialogRef, MAT_DIALOG_DATA, MatDialog } from '@angular/material/dialog'
import { DialogData, FrontRepoService, FrontRepo, SelectionMode } from '../front-repo.service'
import { NullInt64 } from '../null-int64'
import { SelectionModel } from '@angular/cdk/collections';

const allowMultiSelect = true;

import { ActivatedRoute, Router, RouterState } from '@angular/router';
import { FormCellFloat64DB } from '../formcellfloat64-db'
import { FormCellFloat64Service } from '../formcellfloat64.service'

// insertion point for additional imports

import { RouteService } from '../route-service';

// TableComponent is initilizaed from different routes
// TableComponentMode detail different cases 
enum TableComponentMode {
  DISPLAY_MODE,
  ONE_MANY_ASSOCIATION_MODE,
  MANY_MANY_ASSOCIATION_MODE,
}

// generated table component
@Component({
  selector: 'app-formcellfloat64stable',
  templateUrl: './formcellfloat64s-table.component.html',
  styleUrls: ['./formcellfloat64s-table.component.css'],
})
export class FormCellFloat64sTableComponent implements OnInit {

  @Input() GONG__StackPath: string = ""

  // mode at invocation
  mode: TableComponentMode = TableComponentMode.DISPLAY_MODE

  // used if the component is called as a selection component of FormCellFloat64 instances
  selection: SelectionModel<FormCellFloat64DB> = new (SelectionModel)
  initialSelection = new Array<FormCellFloat64DB>()

  // the data source for the table
  formcellfloat64s: FormCellFloat64DB[] = []
  matTableDataSource: MatTableDataSource<FormCellFloat64DB> = new (MatTableDataSource)

  // front repo, that will be referenced by this.formcellfloat64s
  frontRepo: FrontRepo = new (FrontRepo)

  // displayedColumns is referenced by the MatTable component for specify what columns
  // have to be displayed and in what order
  displayedColumns: string[];

  // for sorting & pagination
  @ViewChild(MatSort)
  sort: MatSort | undefined
  @ViewChild(MatPaginator)
  paginator: MatPaginator | undefined;

  ngAfterViewInit() {

    // enable sorting on all fields (including pointers and reverse pointer)
    this.matTableDataSource.sortingDataAccessor = (formcellfloat64DB: FormCellFloat64DB, property: string) => {
      switch (property) {
        case 'ID':
          return formcellfloat64DB.ID

        // insertion point for specific sorting accessor
        case 'Name':
          return formcellfloat64DB.Name;

        case 'Value':
          return formcellfloat64DB.Value;

        default:
          console.assert(false, "Unknown field")
          return "";
      }
    };

    // enable filtering on all fields (including pointers and reverse pointer, which is not done by default)
    this.matTableDataSource.filterPredicate = (formcellfloat64DB: FormCellFloat64DB, filter: string) => {

      // filtering is based on finding a lower case filter into a concatenated string
      // the formcellfloat64DB properties
      let mergedContent = ""

      // insertion point for merging of fields
      mergedContent += formcellfloat64DB.Name.toLowerCase()
      mergedContent += formcellfloat64DB.Value.toString()

      let isSelected = mergedContent.includes(filter.toLowerCase())
      return isSelected
    };

    this.matTableDataSource.sort = this.sort!
    this.matTableDataSource.paginator = this.paginator!
  }

  applyFilter(event: Event) {
    const filterValue = (event.target as HTMLInputElement).value;
    this.matTableDataSource.filter = filterValue.trim().toLowerCase();
  }

  constructor(
    private formcellfloat64Service: FormCellFloat64Service,
    private frontRepoService: FrontRepoService,

    // not null if the component is called as a selection component of formcellfloat64 instances
    public dialogRef: MatDialogRef<FormCellFloat64sTableComponent>,
    @Optional() @Inject(MAT_DIALOG_DATA) public dialogData: DialogData,

    private router: Router,
    private activatedRoute: ActivatedRoute,

    private routeService: RouteService,
  ) {

    // compute mode
    if (dialogData == undefined) {
      this.mode = TableComponentMode.DISPLAY_MODE
    } else {
      this.GONG__StackPath = dialogData.GONG__StackPath
      switch (dialogData.SelectionMode) {
        case SelectionMode.ONE_MANY_ASSOCIATION_MODE:
          this.mode = TableComponentMode.ONE_MANY_ASSOCIATION_MODE
          break
        case SelectionMode.MANY_MANY_ASSOCIATION_MODE:
          this.mode = TableComponentMode.MANY_MANY_ASSOCIATION_MODE
          break
        default:
      }
    }

    // observable for changes in structs
    this.formcellfloat64Service.FormCellFloat64ServiceChanged.subscribe(
      message => {
        if (message == "post" || message == "update" || message == "delete") {
          this.getFormCellFloat64s()
        }
      }
    )
    if (this.mode == TableComponentMode.DISPLAY_MODE) {
      this.displayedColumns = ['ID', 'Delete', // insertion point for columns to display
        "Name",
        "Value",
      ]
    } else {
      this.displayedColumns = ['select', 'ID', // insertion point for columns to display
        "Name",
        "Value",
      ]
      this.selection = new SelectionModel<FormCellFloat64DB>(allowMultiSelect, this.initialSelection);
    }

  }

  ngOnInit(): void {
    let stackPath = this.activatedRoute.snapshot.paramMap.get('GONG__StackPath')
    if (stackPath != undefined) {
      this.GONG__StackPath = stackPath
    }

    this.getFormCellFloat64s()

    this.matTableDataSource = new MatTableDataSource(this.formcellfloat64s)
  }

  getFormCellFloat64s(): void {
    this.frontRepoService.pull(this.GONG__StackPath).subscribe(
      frontRepo => {
        this.frontRepo = frontRepo

        this.formcellfloat64s = this.frontRepo.FormCellFloat64s_array;

        // insertion point for time duration Recoveries
        // insertion point for enum int Recoveries

        // in case the component is called as a selection component
        if (this.mode == TableComponentMode.ONE_MANY_ASSOCIATION_MODE) {
          for (let formcellfloat64 of this.formcellfloat64s) {
            let ID = this.dialogData.ID
            let revPointer = formcellfloat64[this.dialogData.ReversePointer as keyof FormCellFloat64DB] as unknown as NullInt64
            if (revPointer.Int64 == ID) {
              this.initialSelection.push(formcellfloat64)
            }
            this.selection = new SelectionModel<FormCellFloat64DB>(allowMultiSelect, this.initialSelection);
          }
        }

        if (this.mode == TableComponentMode.MANY_MANY_ASSOCIATION_MODE) {

          let mapOfSourceInstances = this.frontRepo[this.dialogData.SourceStruct + "s" as keyof FrontRepo] as Map<number, FormCellFloat64DB>
          let sourceInstance = mapOfSourceInstances.get(this.dialogData.ID)!

          // we associates on sourceInstance of type SourceStruct with a MANY MANY associations to FormCellFloat64DB
          // the field name is sourceField
          let sourceFieldArray = sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance]! as unknown as FormCellFloat64DB[]
          if (sourceFieldArray != null) {
            for (let associationInstance of sourceFieldArray) {
              let formcellfloat64 = associationInstance[this.dialogData.IntermediateStructField as keyof typeof associationInstance] as unknown as FormCellFloat64DB
              this.initialSelection.push(formcellfloat64)
            }
          }

          this.selection = new SelectionModel<FormCellFloat64DB>(allowMultiSelect, this.initialSelection);
        }

        // update the mat table data source
        this.matTableDataSource.data = this.formcellfloat64s
      }
    )
  }

  // newFormCellFloat64 initiate a new formcellfloat64
  // create a new FormCellFloat64 objet
  newFormCellFloat64() {
  }

  deleteFormCellFloat64(formcellfloat64ID: number, formcellfloat64: FormCellFloat64DB) {
    // list of formcellfloat64s is truncated of formcellfloat64 before the delete
    this.formcellfloat64s = this.formcellfloat64s.filter(h => h !== formcellfloat64);

    this.formcellfloat64Service.deleteFormCellFloat64(formcellfloat64ID, this.GONG__StackPath).subscribe(
      formcellfloat64 => {
        this.formcellfloat64Service.FormCellFloat64ServiceChanged.next("delete")
      }
    );
  }

  editFormCellFloat64(formcellfloat64ID: number, formcellfloat64: FormCellFloat64DB) {

  }

  // set editor outlet
  setEditorRouterOutlet(formcellfloat64ID: number) {
    let outletName = this.routeService.getEditorOutlet(this.GONG__StackPath)
    let fullPath = this.routeService.getPathRoot() + "-" + "formcellfloat64" + "-detail"

    let outletConf: any = {}
    outletConf[outletName] = [fullPath, formcellfloat64ID, this.GONG__StackPath]

    this.router.navigate([{ outlets: outletConf }])
  }

  /** Whether the number of selected elements matches the total number of rows. */
  isAllSelected() {
    const numSelected = this.selection.selected.length;
    const numRows = this.formcellfloat64s.length;
    return numSelected === numRows;
  }

  /** Selects all rows if they are not all selected; otherwise clear selection. */
  masterToggle() {
    this.isAllSelected() ?
      this.selection.clear() :
      this.formcellfloat64s.forEach(row => this.selection.select(row));
  }

  save() {

    if (this.mode == TableComponentMode.ONE_MANY_ASSOCIATION_MODE) {

      let toUpdate = new Set<FormCellFloat64DB>()

      // reset all initial selection of formcellfloat64 that belong to formcellfloat64
      for (let formcellfloat64 of this.initialSelection) {
        let index = formcellfloat64[this.dialogData.ReversePointer as keyof FormCellFloat64DB] as unknown as NullInt64
        index.Int64 = 0
        index.Valid = true
        toUpdate.add(formcellfloat64)

      }

      // from selection, set formcellfloat64 that belong to formcellfloat64
      for (let formcellfloat64 of this.selection.selected) {
        let ID = this.dialogData.ID as number
        let reversePointer = formcellfloat64[this.dialogData.ReversePointer as keyof FormCellFloat64DB] as unknown as NullInt64
        reversePointer.Int64 = ID
        reversePointer.Valid = true
        toUpdate.add(formcellfloat64)
      }


      // update all formcellfloat64 (only update selection & initial selection)
      for (let formcellfloat64 of toUpdate) {
        this.formcellfloat64Service.updateFormCellFloat64(formcellfloat64, this.GONG__StackPath)
          .subscribe(formcellfloat64 => {
            this.formcellfloat64Service.FormCellFloat64ServiceChanged.next("update")
          });
      }
    }

    if (this.mode == TableComponentMode.MANY_MANY_ASSOCIATION_MODE) {

      // get the source instance via the map of instances in the front repo
      let mapOfSourceInstances = this.frontRepo[this.dialogData.SourceStruct + "s" as keyof FrontRepo] as Map<number, FormCellFloat64DB>
      let sourceInstance = mapOfSourceInstances.get(this.dialogData.ID)!

      // First, parse all instance of the association struct and remove the instance
      // that have unselect
      let unselectedFormCellFloat64 = new Set<number>()
      for (let formcellfloat64 of this.initialSelection) {
        if (this.selection.selected.includes(formcellfloat64)) {
          // console.log("formcellfloat64 " + formcellfloat64.Name + " is still selected")
        } else {
          console.log("formcellfloat64 " + formcellfloat64.Name + " has been unselected")
          unselectedFormCellFloat64.add(formcellfloat64.ID)
          console.log("is unselected " + unselectedFormCellFloat64.has(formcellfloat64.ID))
        }
      }

      // delete the association instance
      let associationInstance = sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance]
      let formcellfloat64 = associationInstance![this.dialogData.IntermediateStructField as keyof typeof associationInstance] as unknown as FormCellFloat64DB
      if (unselectedFormCellFloat64.has(formcellfloat64.ID)) {
        this.frontRepoService.deleteService(this.dialogData.IntermediateStruct, associationInstance)


      }

      // is the source array is empty create it
      if (sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance] == undefined) {
        (sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance] as unknown as Array<FormCellFloat64DB>) = new Array<FormCellFloat64DB>()
      }

      // second, parse all instance of the selected
      if (sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance]) {
        this.selection.selected.forEach(
          formcellfloat64 => {
            if (!this.initialSelection.includes(formcellfloat64)) {
              // console.log("formcellfloat64 " + formcellfloat64.Name + " has been added to the selection")

              let associationInstance = {
                Name: sourceInstance["Name"] + "-" + formcellfloat64.Name,
              }

              let index = associationInstance[this.dialogData.IntermediateStructField + "ID" as keyof typeof associationInstance] as unknown as NullInt64
              index.Int64 = formcellfloat64.ID
              index.Valid = true

              let indexDB = associationInstance[this.dialogData.IntermediateStructField + "DBID" as keyof typeof associationInstance] as unknown as NullInt64
              indexDB.Int64 = formcellfloat64.ID
              index.Valid = true

              this.frontRepoService.postService(this.dialogData.IntermediateStruct, associationInstance)

            } else {
              // console.log("formcellfloat64 " + formcellfloat64.Name + " is still selected")
            }
          }
        )
      }

      // this.selection = new SelectionModel<FormCellFloat64DB>(allowMultiSelect, this.initialSelection);
    }

    // why pizza ?
    this.dialogRef.close('Pizza!');
  }
}
