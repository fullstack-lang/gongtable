// generated by gong
import { Component, OnInit, Inject, Optional } from '@angular/core';
import { TypeofExpr } from '@angular/compiler';
import { CdkDragDrop, moveItemInArray } from '@angular/cdk/drag-drop';

import { MatDialogRef, MAT_DIALOG_DATA, MatDialog } from '@angular/material/dialog'
import { DialogData } from '../front-repo.service'
import { SelectionModel } from '@angular/cdk/collections';

import { Router, RouterState } from '@angular/router';
import { FormCellBooleanDB } from '../formcellboolean-db'
import { FormCellBooleanService } from '../formcellboolean.service'

import { FrontRepoService, FrontRepo } from '../front-repo.service'
import { NullInt64 } from '../null-int64'

@Component({
  selector: 'lib-formcellboolean-sorting',
  templateUrl: './formcellboolean-sorting.component.html',
  styleUrls: ['./formcellboolean-sorting.component.css']
})
export class FormCellBooleanSortingComponent implements OnInit {

  frontRepo: FrontRepo = new (FrontRepo)

  // array of FormCellBoolean instances that are in the association
  associatedFormCellBooleans = new Array<FormCellBooleanDB>();

  constructor(
    private formcellbooleanService: FormCellBooleanService,
    private frontRepoService: FrontRepoService,

    // not null if the component is called as a selection component of formcellboolean instances
    public dialogRef: MatDialogRef<FormCellBooleanSortingComponent>,
    @Optional() @Inject(MAT_DIALOG_DATA) public dialogData: DialogData,

    private router: Router,
  ) {
    this.router.routeReuseStrategy.shouldReuseRoute = function () {
      return false;
    };
  }

  ngOnInit(): void {
    this.getFormCellBooleans()
  }

  getFormCellBooleans(): void {
    this.frontRepoService.pull(this.dialogData.GONG__StackPath).subscribe(
      frontRepo => {
        this.frontRepo = frontRepo

        let index = 0
        for (let formcellboolean of this.frontRepo.FormCellBooleans_array) {
          let ID = this.dialogData.ID
          let revPointerID = formcellboolean[this.dialogData.ReversePointer as keyof FormCellBooleanDB] as unknown as NullInt64
          let revPointerID_Index = formcellboolean[this.dialogData.ReversePointer + "_Index" as keyof FormCellBooleanDB] as unknown as NullInt64
          if (revPointerID.Int64 == ID) {
            if (revPointerID_Index == undefined) {
              revPointerID_Index = new NullInt64
              revPointerID_Index.Valid = true
              revPointerID_Index.Int64 = index++
            }
            this.associatedFormCellBooleans.push(formcellboolean)
          }
        }

        // sort associated formcellboolean according to order
        this.associatedFormCellBooleans.sort((t1, t2) => {
          let t1_revPointerID_Index = t1[this.dialogData.ReversePointer + "_Index" as keyof typeof t1] as unknown as NullInt64
          let t2_revPointerID_Index = t2[this.dialogData.ReversePointer + "_Index" as keyof typeof t2] as unknown as NullInt64
          if (t1_revPointerID_Index && t2_revPointerID_Index) {
            if (t1_revPointerID_Index.Int64 > t2_revPointerID_Index.Int64) {
              return 1;
            }
            if (t1_revPointerID_Index.Int64 < t2_revPointerID_Index.Int64) {
              return -1;
            }
          }
          return 0;
        });
      }
    )
  }

  drop(event: CdkDragDrop<string[]>) {
    moveItemInArray(this.associatedFormCellBooleans, event.previousIndex, event.currentIndex);

    // set the order of FormCellBoolean instances
    let index = 0

    for (let formcellboolean of this.associatedFormCellBooleans) {
      let revPointerID_Index = formcellboolean[this.dialogData.ReversePointer + "_Index" as keyof FormCellBooleanDB] as unknown as NullInt64
      revPointerID_Index.Valid = true
      revPointerID_Index.Int64 = index++
    }
  }

  save() {

    this.associatedFormCellBooleans.forEach(
      formcellboolean => {
        this.formcellbooleanService.updateFormCellBoolean(formcellboolean, this.dialogData.GONG__StackPath)
          .subscribe(formcellboolean => {
            this.formcellbooleanService.FormCellBooleanServiceChanged.next("update")
          });
      }
    )

    this.dialogRef.close('Sorting of ' + this.dialogData.ReversePointer + ' done');
  }
}
