// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { FormCellFloat64DB } from './formcellfloat64-db';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class FormCellFloat64Service {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  FormCellFloat64ServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private formcellfloat64sUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.formcellfloat64sUrl = origin + '/api/github.com/fullstack-lang/gongtable/go/v1/formcellfloat64s';
  }

  /** GET formcellfloat64s from the server */
  getFormCellFloat64s(GONG__StackPath: string): Observable<FormCellFloat64DB[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<FormCellFloat64DB[]>(this.formcellfloat64sUrl, { params: params })
      .pipe(
        tap(),
		// tap(_ => this.log('fetched formcellfloat64s')),
        catchError(this.handleError<FormCellFloat64DB[]>('getFormCellFloat64s', []))
      );
  }

  /** GET formcellfloat64 by id. Will 404 if id not found */
  getFormCellFloat64(id: number, GONG__StackPath: string): Observable<FormCellFloat64DB> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.formcellfloat64sUrl}/${id}`;
    return this.http.get<FormCellFloat64DB>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched formcellfloat64 id=${id}`)),
      catchError(this.handleError<FormCellFloat64DB>(`getFormCellFloat64 id=${id}`))
    );
  }

  /** POST: add a new formcellfloat64 to the server */
  postFormCellFloat64(formcellfloat64db: FormCellFloat64DB, GONG__StackPath: string): Observable<FormCellFloat64DB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FormCellFloat64DB>(this.formcellfloat64sUrl, formcellfloat64db, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`posted formcellfloat64db id=${formcellfloat64db.ID}`)
      }),
      catchError(this.handleError<FormCellFloat64DB>('postFormCellFloat64'))
    );
  }

  /** DELETE: delete the formcellfloat64db from the server */
  deleteFormCellFloat64(formcellfloat64db: FormCellFloat64DB | number, GONG__StackPath: string): Observable<FormCellFloat64DB> {
    const id = typeof formcellfloat64db === 'number' ? formcellfloat64db : formcellfloat64db.ID;
    const url = `${this.formcellfloat64sUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<FormCellFloat64DB>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted formcellfloat64db id=${id}`)),
      catchError(this.handleError<FormCellFloat64DB>('deleteFormCellFloat64'))
    );
  }

  /** PUT: update the formcellfloat64db on the server */
  updateFormCellFloat64(formcellfloat64db: FormCellFloat64DB, GONG__StackPath: string): Observable<FormCellFloat64DB> {
    const id = typeof formcellfloat64db === 'number' ? formcellfloat64db : formcellfloat64db.ID;
    const url = `${this.formcellfloat64sUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<FormCellFloat64DB>(url, formcellfloat64db, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`updated formcellfloat64db id=${formcellfloat64db.ID}`)
      }),
      catchError(this.handleError<FormCellFloat64DB>('updateFormCellFloat64'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in FormCellFloat64Service', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("FormCellFloat64Service" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
      console.log(message)
  }
}
